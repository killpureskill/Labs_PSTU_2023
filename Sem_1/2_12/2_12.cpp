#include <iostream>
using namespace std;
int main() {
	int n, x, max = -10000, min = 10000;
	cin >> n;

	for (int i = 0; i < n; i++) {
		cin >> x;
		if (x >= max_0) {
			max = x;
		}
		if (x <= min) {
			min = x;
		}
	}
	cout << max + min << endl;
}